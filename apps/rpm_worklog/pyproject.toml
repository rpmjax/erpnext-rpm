[project]
name = "rpm_worklog"
version = "0.1.0"
description = "RPM Work Log review and validation"
authors = [{name = "RPM"}]
requires-python = ">=3.14"
[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"
