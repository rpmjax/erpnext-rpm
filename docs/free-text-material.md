# 工作內容／物料：自由文字與物料候選

2026-09-17：依使用者要求，重新啟用既有 work_item Data 欄位，標示「工作內容／物料」，取代列表中的 item_code Link 輸入。item_code 仍保留為有效 Item 關聯，不接受不存在的主檔，不建立新 Item。

- 可直接輸入自由文字並保存，不需選取物料。
- 輸入代碼／品名後延遲 250ms 查詢既有受控物料 API，最多 20 個候選；點選才寫入 Item 關聯。
- 保留搜尋按鈕作為另一入口；解除關聯保留工作文字。
- 修改原已關聯列的工作文字會解除原關聯。伺服器亦檢查既有列文字變更，避免僅靠前端。
- 主管查詢顯示自由文字。以物料分組的報表仍依真實 item_code，自由文字列屬未指定物料，不被當成主檔。
- Migration 僅補齊舊資料中空白的 work_item，從原 item_code／名稱快照組成顯示文字（最多 140 字）；已有文字、物料關聯與審核狀態不變。
- 原生 Data 欄位最多 140 字，較長說明請放備註。單位與記錄數量開關仍停用。

驗證：本機映像建置與 migrate 通過；test_items_poc.py 回滾測試通過（自由文字存取、變更自動解聯、原關聯快照、鎖定紀錄、停用物料及 Guest 拒絕）；metadata 及 JavaScript 語法檢查通過。瀏覽器操作由使用者手動驗收。

手動驗收：
1. 重新載入並新增草稿，輸入「跨部門協助」、結果與工時，保存再開啟，文字應保留。
2. 另一列輸入 sho，點選候選，保存後應保留 Item 及名稱快照。
3. 將已選物料的文字改為自由內容，保存後原關聯清空而文字保留。
4. 主管查詢能看到自由內容；已送審／核准單仍不可修改。

VM 更新使用正常 git pull、build、update、status；不要 init。8083 已更新，VM 由使用者維護時段更新。
